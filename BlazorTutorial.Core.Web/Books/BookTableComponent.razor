@if (Books == null)
{
    <p><em>Loading...</em></p>
}
else if (!Books.Any())
{
    <p><em>No books found</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Overview</th>
                <th>Author</th>
                <th>Go To</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var book in Books)
            {
                <tr>
                    <td>@book.Title</td>
                    <td>@book.Overview</td>
                    @if (book.Author != null)
                    {
                        <td><a href="/profile/@book.Author.Id">@book.Author.DisplayName</a></td>
                    }
                    else
                    {
                        <td>Unknown</td>
                    }
                    <td><a href="/book/@book.Id">View</a></td>
                </tr>
            }
        </tbody>
    </table>
}
